#!/bin/bash

bold=$(tput bold) norm=$(tput sgr0)

case "$1" in
    --help|-h) printf "
See ${bold}man dpkg-buildpackage${norm} for details.

";;

    *) dpkg-buildpackage --build=source --no-check-builddeps --no-pre-clean "$@";;
esac
