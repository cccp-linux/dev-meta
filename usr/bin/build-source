#!/bin/bash
case "$1" in
    --help|-h) printf "\nSee `tput bold`man dpkg-buildpackage`tput sgr0` for details.\n\n";;

    *) dpkg-buildpackage --build=source --no-check-builddeps --no-pre-clean "$@";;
esac
